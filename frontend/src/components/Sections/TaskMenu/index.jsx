import React from 'react';
import MenuHeader from './MenuHeader';

function TaskMenu() {
	return (
		<section className="task__centerMenu">
			<MenuHeader />
		</section>
	);
}

export default TaskMenu;
